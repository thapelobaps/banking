# Next.js web application
NEXT_PUBLIC_SITE_URL=http://localhost:3000
KAPE_API_URL=http://localhost:5000

# The ASP.NET Core API reads its SQL Server connection and JWT signing key
# from backend configuration or environment variables. Do not place backend
# secrets in this frontend file.
